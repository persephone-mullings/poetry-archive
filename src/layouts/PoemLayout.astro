---
import BaseLayout from "./BaseLayout.astro";

const { pageTitle, description } = Astro.props;
---

<BaseLayout pageTitle={pageTitle} description={description} contentClass="poem-page">
  <slot />
</BaseLayout>
